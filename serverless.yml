service: ingredofit-aws-lambda-function
provider:
  name: aws
  runtime: nodejs10.x
functions:
  hello:
    handler: handler.hello
    events:
     - http:
         path: users/create
         method: get